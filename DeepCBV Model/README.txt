Script to generate Deep CBV

Note: make sure that all paths end with '/'. also use script from this directory.

Args:

--inp: input path with .nii.gz files.
--output: output path for aCBV
--model_path: path to best performing model. ok to leave blank since default is set to the current best.
--device: gpu index